{
  "dependencies": {
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "isomorphic-dompurify": "^4.1.0",
    "jsonwebtoken": "^9.0.3",
    "multer": "^2.2.0",
    "nodemailer": "^9.0.3",
    "pg": "^8.22.0"
  },
  "name": "backend",
  "version": "1.0.0",
  "description": "REST API untuk monitoring aset IT",
  "main": "src/server.js",
  "devDependencies": {
    "nodemon": "^3.1.14"
  },
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon --watch .env --watch src src/server.js",
    "db:check": "node src/config/checkSchema.js",
    "test": "node --test",
    "check": "node --check src/server.js && node --check src/app.js && node --check src/config/setupDatabase.js && node --check src/config/checkSchema.js",
    "db:migrate:plan": "node src/config/migrateDatabase.js --plan",
    "db:migrate:apply": "node src/config/migrateDatabase.js --apply"
  },
  "engines": {
    "node": "^22.18.0 || >=24.12.0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module"
}
