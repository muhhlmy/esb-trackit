# Docker Compose & Root Environment Configuration
# Copy to .env before running: docker compose up

# PostgreSQL Credentials (Wajib diisi & aman)
DB_USER=postgres
DB_PASSWORD=GantiDenganPasswordDatabaseYangKuat123!
DB_NAME=assets_monitoring
DB_PORT=5432

# Backend Security & JWT (Wajib diisi minimal 32 karakter acak)
JWT_SECRET=GantiDenganSecretAcakMinimal32KarakterYangKuatDanAman!

# Host & Ports
PORT=3000

# Public application origin (HTTPS origin for production behind TLS termination)
CORS_ORIGINS=http://localhost
TRUST_PROXY_CIDRS=

# Required for account activation and password-reset email delivery
EMAIL_ENABLED=false
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
EMAIL_FROM=
